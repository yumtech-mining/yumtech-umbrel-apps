[project]
name = "yumtech-ckpool-dashboard"
version = "1.0.2"
description = "Read-only YUMTECH dashboard for a native CKPool installation"
requires-python = ">=3.11"
license = {text = "MIT"}

[project.scripts]
yumtech-ckpool-dashboard = "yumtech_dashboard.__main__:main"

[tool.unittest]
start-directory = "tests"
